package com.moments.android.views.messaging.services

/** Punto de extensión para instrumentación de scroll, desactivado como en iOS. */
object ChatScrollDebug {
    fun log(message: String) = Unit
}
